# Orchestration Log: Batty — Alpha.2 Implementation

**Timestamp:** 2026-06-01T07:00:47Z  
**Agent:** batty (claude-opus-4.6)  
**Mode:** background  
**Status:** ✅ Completed successfully

## Task Summary

Implemented 3 Alpha.2 blockers for ratify v2.0.0-alpha.2 deployment readiness:

1. **Health endpoints** (`/healthz`, `/readyz`) — Kubernetes-idiomatic probes at flat paths, registered before verify/mutate handlers
2. **StoreMux fallback** — `"*"` scope convention for zero-config global fallback; maps to `StoreMux.RegisterFallback()`
3. **Cert rotation watcher** — Dual fsnotify + SHA-256 polling (30s interval) for K8s symlink resilience with 2s debounce

## Decisions Made

- Health probes: flat paths, no auth middleware wrapping
- Scope `"*"`: explicit fallback registration, non-breaking addition to existing APIs
- Cert watcher: immediate fsnotify response + periodic polling for robustness
- Readiness signal: goroutine polling `getExecutor() != nil` for simplicity

## Artifacts Created

- `src/internal/httpserver/health.go` + `health_test.go`
- `src/internal/httpserver/server_modifications.go`
- `src/internal/store/factory_modifications.go` + `factory_test.go`
- `src/internal/executor/executor_modifications.go`
- `src/internal/verifier/truststore/watcher.go` + `watcher_test.go`

## Impact

- No breaking changes to existing APIs/configs
- Unblocks Kubernetes deployment without workarounds
- ~5 new files, ~6 modified files

## Next Steps

- Team review of design doc (`ratify-upstream/alpha2-design.md`)
- Deckard approval for merge to main
